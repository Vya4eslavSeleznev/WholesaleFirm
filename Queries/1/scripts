INSERT INTO GOODS (NAME, PRIORITY)
VALUES ('Qwerty2', 0)

INSERT INTO SALES (GOOD_ID, GOOD_COUNT, CREATE_DATE)
VALUES (3, 11, '01-02-2021')

INSERT INTO WAREHOUSE2 (GOOD_ID, GOOD_COUNT)
VALUES (1, 10000)

SELECT * FROM GOODS

ALTER TABLE WAREHOUSE2 DROP CONSTRAINT WAREHOUSE2_GOOD_ID

ALTER TABLE WAREHOUSE2
ADD FOREIGN KEY (GOOD_ID) REFERENCES GOODS(ID);